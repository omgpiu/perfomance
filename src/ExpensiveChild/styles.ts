import {styled} from "linaria/react";
import {FlexWrapper} from "../styles";

export const ExpensiveChildComponent = styled(FlexWrapper)`
  display: flex;
  gap: 20px;
  padding: 100px 20px 20px 20px;
  justify-content: space-around;
`
